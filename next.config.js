/** @type {import('next').NextConfig} */
const nextConfig = {
  // Si en el futuro necesitas un subdirectorio, puedes añadir aquí:
  // basePath: '/tu-subdirectorio',
};
 
module.exports = nextConfig;